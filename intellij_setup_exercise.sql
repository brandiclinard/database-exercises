# describe departments;

# select * from departments; /*returns 9 rows --col: dept_no, dept_name */

# select * from dept_emp; /*returns 500 rows-- col: emp_no, dept_no, from_date, to_date*/

# select * from dept_manager; /*returns 24 rows-- col: dept_no, emp_no, from_date, to_date*/

# select * from employees; /*returns 500 rows-- col: emp_no, birth_date, first_name, last_name, gender, hire_date*/

# select * from salaries; /*returns 500 rows-- col: emp_no, salary, from_date, to_date*/

# select * from titles; /*returns 500 rows-- col: emp_no, title, from_date, to_date*/